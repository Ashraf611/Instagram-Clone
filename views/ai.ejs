<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Chat</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.3/css/bootstrap.min.css">
    <style>
        body {
            background-color: #fafafa;
            font-family: 'Arial', sans-serif;
            color: #333;
        }

        .container {
            max-width: 600px;
            background-color: #fff;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-top: 80px;
        }

        h1 {
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 20px;
            font-weight: 600;
        }

        .btn-danger {
            background-color: #f44336;
            border-radius: 30px;
            border: none;
            padding: 10px 20px;
            font-weight: 600;
            text-transform: uppercase;
            display: block;
            width: 100px;
            margin: 10px auto;
        }

        .btn-danger:hover {
            background-color: #d32f2f;
        }

        form {
            margin-top: 20px;
        }

        .form-label {
            font-size: 1.1rem;
            font-weight: 500;
        }

        .form-control {
            border-radius: 30px;
            padding: 15px;
            font-size: 1.1rem;
            background-color: #f1f1f1;
            border: 1px solid #ddd;
        }

        .form-control:focus {
            border-color: #f44336;
            background-color: #fff;
        }

        .btn-primary {
            border: none;
            padding: 12px 30px;
            border-radius: 30px;
            font-size: 1rem;
            width: 100%;
            cursor: pointer;
        }

        .btn-primary:hover {
            background-color: #021f77;
        }

        .ai-response {
            background-color: #f7f7f7;
            border-left: 5px solid #36d8f4;
            padding: 20px;
            margin-top: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
        }

        .ai-response strong {
            color: #36a8f4;
            font-size: 1.2rem;
        }

        .alert {
            border-radius: 8px;
            padding: 15px;
            font-size: 1rem;
            margin-top: 20px;
            text-align: center;
        }

        .alert-success {
            background-color: rgba(76, 175, 80, 0.1);
            color: #4CAF50;
        }

        .alert-danger {
            background-color: rgba(244, 67, 54, 0.1);
            color: #36abf4;
        }

        .response-text {
            font-size: 1.1rem;
            color: #333;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <a class="btn btn-danger" href="/home">Back</a>
        <h1>My AI Chat</h1>
        <form action="/home/ask" method="POST">
            <div class="mb-3">
                <label for="message" class="form-label">Ask AI:</label>
                <input type="text" class="form-control" name="message" id="message" required>
            </div>
            <button type="submit" class="btn btn-primary">Ask</button>
        </form>

        <% if (response) { %>
            <div class="ai-response">
                <strong>AI:</strong>
                <div class="response-text"><%= response %></div>
            </div>
        <% } %>

        <% if (error) { %>
            <div class="alert alert-danger">
                <strong>Error:</strong> <%= error %>
            </div>
        <% } %>
    </div>
</body>
</html>
